plugins {
    id "com.android.application"
}

android {
    namespace "com.tietie.diary"
    compileSdk 35

    buildFeatures {
        buildConfig true
    }

    defaultConfig {
        applicationId "com.tietie.diary"
        minSdk 24
        targetSdk 35
        versionCode 1
        versionName "0.1.0"
        buildConfigField "String", "TIETIE_WEB_URL", "\"http://10.0.2.2:5173\""
    }
}
